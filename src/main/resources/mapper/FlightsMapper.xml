<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ta.mapper.FlightsMapper">
    <select id="getFlightsList" resultType="java.util.HashMap">
        SELECT ID as "id", AIRLINE as "airline", DEPARTURE as "departure", DESTINATION as "destination",
        FLYTIME as "flytime", CLASS as "class", OFF as "off", ORIGINAL as "original" FROM flight
    </select>
</mapper>